---
server:
    port: 8082
spring:
    datasource:
        url: jdbc:mysql://localhost/loans?createDatabaseIfNotExist=true&autoReconnect=true&useSSL=false&allowPublicKeyRetrieval=true
        #driverClassName: org.h2.Driver
        password: 'root'
        username: 'root'
       # url: jdbc:h2:mem:testdb

    #h2:
        #console:
            #enabled: true
    jpa:
        #database-platform: org.hibernate.dialect.
        databasePlatform: org.hibernate.dialect.MySQL5InnoDBDialect

        hibernate:

            ddl-auto: update
        show-sql: true
