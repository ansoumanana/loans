---
server:
    port: 7000
spring:
    application:
        name: loans
    profiles:
        active: prod
    config:
        import: optional:configserver:http://configserver:8070/
management:
    auditevents:
        enabled: true
    endpoints:
        web:
            exposure:
                include: *
